defmodule DemoAphWeb.PageControllerTest do
  use DemoAphWeb.ConnCase

#  test "GET /", %{conn: conn} do
#    conn = get conn, "/"
#    x = html_response(conn, 200)
#    :io.format("~nresp: ~p~n", [x])
##    assert html_response(conn, 200) =~ "Name"
#  end
end
